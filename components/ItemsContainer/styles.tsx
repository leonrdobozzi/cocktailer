import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  itemsContainer: {
    paddingLeft: 24,
    minHeight: 200,
    marginTop: 30,
  },
  itemsContainerTitle: {
    color: "#fff",
    fontSize: 10,
    marginBottom: 10,
    fontFamily: "MontserratBold",
  },
});
